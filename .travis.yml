language: python
python:
  - "2.6"
  - "2.7"
# install cython before regular install
before_install:
  - pip install --use-mirrors Cython
# command to install dependencies
install: "pip install -r requirements.txt --use-mirrors"
# command to run tests
script: nosetests
# services
services: sqlite3
